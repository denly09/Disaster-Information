<h1>Post List</h1>
<table class="table">
  <thead>
  <td>id</td>
  <td>title</td>
  <td>content</td>
  <td>user</td>
  <td>address</td>
  <td>Category</td>
  <td>action</td>
  </thead>
  <% @posts.each do |post| %>
    <tr>
      <td><%= post.id %></td>
      <td><%= post.categories.pluck(:name).join(',') %></td>
      <td><%= post.title %></td>
      <td><%= post.content %></td>
      <td><%= post.address %></td>
      <td><%= post.user.email %></td>
      <td>
        <%= link_to 'Show', post_path(post) %>
        <%= link_to 'Edit', edit_post_path(post) %>
        <%= link_to 'comments', post_comments_path(post) %>
        <%= link_to 'new comment', new_post_comment_path(post) %>
        <%= button_to 'Delete', post_path(post), method: :delete %>

      </td>
    </tr>
  <% end %>
</table>

<%= link_to 'Create New Disaster Information', new_post_path %>